services:
  asciidoc_app:
    build:
      context: ./
      dockerfile: Dockerfile
    image: asciidoc_img:latest
    container_name: asciidoc_container
    # NOTE: localに生成したhtmlを反映するために必要
    volumes:
      - ./:/app
